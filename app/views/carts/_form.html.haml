/ = form_for(@cart) do |f|
/   - if @cart.errors.any?
/     #error_explanation
/       %h2
/         = pluralize(@cart.errors.count, "error")
/         prohibited this cart from being saved:
/       %ul
/         - @cart.errors.full_messages.each do |msg|
/           %li= msg
/   .actions
/     = f.submit

= form_for @cart, :html => {:class => "form-horizontal"} do |f|
  / .form-group
  /   %label.col-sm-3.control-label.no-padding-right{:for => "form-field-1"} Name
  /   .col-sm-9
  /     = f.text_field :name, :class => "col-xs-10 col-sm-5", :placeholder => "Name"
  / .form-group
  /   %label.col-sm-3.control-label.no-padding-right{:for => "form-field-1"} Code
  /   .col-sm-9
  /     = f.text_field :code, :class => "col-xs-10 col-sm-5", :placeholder => "Code"
  / .form-group
  /   %label.col-sm-3.control-label.no-padding-right{:for => "form-field-1"} Description
  /   .col-sm-9
  /     = f.text_field :description, :class => "col-xs-10 col-sm-5", :placeholder => "Description"
  / .space-4
  / .clearfix.form-actions
  /   .col-md-offset-3.col-md-9
  - if @cart.errors.any?
    #error_explanation
      %h2
        = pluralize(@cart.errors.count, "error")
        prohibited this cart from being saved:
      %ul
        - @cart.errors.full_messages.each do |msg|
          %li= msg
  .actions
    = f.submit "Submit", :class => "btn btn-info"
