%table#simple-table.table.table-striped.table-bordered.table-hover
  %thead
    %tr
      %th Title
      %th Description
      %th Price
      %th Category_id
      %th Uniq_Code
      %th Code
      %th Color
  %tbody
  - @products.each do |product|
    %tr
      %td= product.title
      %td= product.description
      %td= product.price
      %td= product.category_id
      %td= product.uniq_code
      %td= product.code
      %td= product.color
      %td{:width => "5%"}= link_to 'Show', product
      %td{:width => "5%"}= link_to 'Edit', edit_product_path(product)
      %td{:width => "5%"}= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' }
%br/

